<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wslclds.castn">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">

        <!-- Services -->
        <service
            android:name="com.wslclds.castn.services.AudioPlayerService"
            android:exported="false" />
        <service
            android:name="com.wslclds.castn.services.OPMLImportService"
            android:exported="false" />
        <service
            android:name="com.wslclds.castn.services.DownloadService"
            android:exported="false" />
        <service
            android:name="com.wslclds.castn.services.FindNewEpisodesService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.firebase.jobdispatcher.ACTION_EXECUTE" />
            </intent-filter>
        </service>
        <!-- Activities -->
        <activity
            android:name="com.wslclds.castn.activities.MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <action android:name="android.intent.action.VIEW" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="castn" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.wslclds.castn.activities.EpisodeQueueActivity"
            android:theme="@style/Popup" />
        <activity
            android:name="com.wslclds.castn.activities.EpisodeDetailActivity"
            android:theme="@style/Popup" />
    </application>

</manifest>